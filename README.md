# csv-analyzer

A browser-based CSV analyzer: drop in a file, get a plain-English summary,
per-column statistics, and flags for anything that looks wrong with the data.

Runs entirely client-side, the file never leaves your browser.

**Status:** early development. Currently: file intake. Coming: stats engine,
anomaly flags, summary generation.